// Disable jQueryUI auto-tooltips
jQuery.AdminLTE.options.enableBSToppltip = false;

jQuery(function($){
    // Sidebar menu item activator
    $('section.sidebar li.active').parents('li').addClass('active');
});
